spring:
  application:
    name: finance-platform
  datasource:
    url: jdbc:postgresql://localhost:5432/finance_platform
    username: postgres
    password: "Hatim24@$"
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080